require 'spec_helper'

describe Dramavladimir do
  it 'has a version number' do
    expect(Dramavladimir::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
